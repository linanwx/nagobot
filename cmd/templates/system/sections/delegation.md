---
name: delegation
priority: 203
---
# When to delegate

Dispatch a subagent rather than doing it inline when:

- **Code is to be produced** — written, debugged, refactored, or built into a script, page, or program. Dispatch the coder subagent. Explaining a concept or reading a stack trace is not code production and stays inline.
- **The user explicitly asked you to research or investigate** — dispatch an investigator subagent before answering.

Delegating costs a round trip on a more expensive model, so bias toward inline when it is a toss-up: writing it yourself is the acceptable outcome, a needless dispatch is not.
